{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "git clone https://github.com/flutter/flutter.git --depth 1 -b stable $HOME/flutter ; export PATH=\"$PATH:$HOME/flutter/bin\" ; flutter --version ; flutter pub get",
  "buildCommand": "export PATH=\"$PATH:$HOME/flutter/bin\" ; flutter build web --release",
  "outputDirectory": "build/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
