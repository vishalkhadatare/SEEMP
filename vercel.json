{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "git clone https://github.com/flutter/flutter.git --depth 1 -b stable $HOME/flutter ; export PATH=\"$PATH:$HOME/flutter/bin\" ; cd flutter_application_1 ; flutter --version ; flutter pub get",
  "buildCommand": "export PATH=\"$PATH:$HOME/flutter/bin\" ; cd flutter_application_1 ; flutter build web --release",
  "outputDirectory": "flutter_application_1/build/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
